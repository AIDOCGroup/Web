div.title-right
    button.btn.btn-primary.btn-addon(ng-click='mv.update()')
        i.fa.fa-plus
        | 添加管理员
div.panel.panel-default
    div.panel-heading.font-bold 管理员列表
    div.table-responsive

        table.table.table-striped.m-b-none
            thead
                tr
                    th 名称
                    th 手机号
                    th 创建时间
                    th.text-right 操作
            tbody
                tr(ng-repeat='item in mv.items') 
                    td {{item.realName}}
                    td {{item.phoneNumber}}
                    td {{item.created}}
                    td.text-right
                        button.btn.btn-default.btn-sm(ng-click='mv.update(item)') 修改
                        button.btn.btn-default.btn-sm.m-l-sm(ng-click='mv.remove(item)') 删除